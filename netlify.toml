# Configuración para Netlify
# Git Gateway para autenticación con Decap CMS

[build]
  publish = "."
  command = "echo 'Static site - no build required'"

# Redireccionamiento para el admin panel
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200

